project.gradle.startParameter.excludedTaskNames.addAll("test", "publish", "javadocJar", "sourcesJar", "jar", "check")

subprojects {
    apply plugin: "java"
    apply plugin: "org.springframework.boot:3.2.2"
}